[pytest]
testpaths = tests
pythonpath = .
markers =
    windows_only: exercises Windows-specific OS semantics (os.replace/PermissionError). Skipped by default outside win32.
    live_store: touches a real on-disk store across process runs (not a synthetic fixture). Excluded from default runs.
addopts = -m "not windows_only and not live_store"
